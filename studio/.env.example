# Base URL da API usada pelo frontend em produção.
# Exemplo: https://api.seudominio.com
# Se usar reverse proxy no mesmo domínio, deixe como /api
VITE_API_BASE_URL=/api

# URL da API para o proxy do Vite em desenvolvimento local
VITE_DEV_API_TARGET=http://api:8000
