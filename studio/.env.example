# Build-time fallback da API usada pelo frontend.
# Em produção com container, prefira configurar API_BASE_URL (runtime)
# no serviço do EasyPanel.
VITE_API_BASE_URL=/api

# URL da API para o proxy do Vite em desenvolvimento local
VITE_DEV_API_TARGET=http://api:8000
